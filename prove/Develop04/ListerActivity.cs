class ListerActivity : Activity
{
   ListerActivity(string title, string desc, int duration) : base(title, desc, duration){
    _title = title;
    _desc = desc;
    _duration = duration;
   }

   public void InteractLister(){

   }
}