using Microsoft.VisualBasic;

class BreathingActivity : Activity
{
    public BreathingActivity(string title, string desc, int duration) : base(title, desc, duration){
        _title = title;
        _desc = desc;
        _duration = duration;
    }

    public void InteractBreathing(){
        
    }
}